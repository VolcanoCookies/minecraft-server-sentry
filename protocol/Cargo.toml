[package]
name = "protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
linkme = "0.3.32"
packet = { path = "../packet" }
packet_macros = { path = "../packet_macros" }
serde = { workspace = true }
serde_json = { workspace = true }
